<a href="{{ link }}" class="card feature-card no-margin">
    <div class="card-content card-content-padding text-align-center">
        <div class="color-primary">
            {% block content %}{% endblock %}
        </div>
        <p class="feature-title">{{ title|raw }}</p>
    </div>
</a>
