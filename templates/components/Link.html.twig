<a {{ attributes.defaults({class: 'external'}) }}
    {{ attributes}}
    href="{{ href }}"
    {% if title is defined %}
    title="{{ title }}"
    {% endif %}
>
    {% block content %}{% endblock %}
</a>
