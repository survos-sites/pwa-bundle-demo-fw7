<template>
    <div class="page">

        <div class="navbar">
            <div class="navbar-bg"></div>
            <div class="navbar-inner">
                <div class="left">
                    <a href="#" class="link back">
                        <i class="icon icon-back"></i>
                        <span class="ios-only">Back</span>
                    </a>
                </div>
                <div class="title">Multitouch</div>
            </div>
        </div>

        <div class="page-content">

            <div class="block-title block-title-large">Multitouch</div>
            <div class="block">
                <p>
                    On a device with touch screen, place two fingers in the square below to see a red circle appear. <br/>
                    Move your fingers to change the size and position of the circle.
                </p>
            </div>

            <div style="height: 100%;">
                <canvas
                    id="canvas"
                    style="width: 100%; height: 350px; background-color: #1da1f2;"
                    data-controller="pwa--touch multitouch"
                    {{ stimulus_action('multitouch', 'update', 'pwa--touch:pwa:touch:updated') }}
                ></canvas>
            </div>

        </div>

    </div>
</template>

<script>
    export default function(props, {$, $el, $f7, $f7route, $f7router, $h, $on, $store, $theme, $update}) {
        return $render;
    }
</script>
