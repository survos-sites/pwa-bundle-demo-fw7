<template>
    <div class="page" data-controller="pwa--wake-lock">

        <div class="navbar">
            <div class="navbar-bg"></div>
            <div class="navbar-inner">
                <div class="left">
                    <a href="#" class="link back">
                        <i class="icon icon-back"></i>
                        <span class="ios-only">Back</span>
                    </a>
                </div>
                <div class="title">Wake lock</div>
            </div>
        </div>

        <div class="page-content">

            <div class="block-title block-title-large">Wake lock</div>
            <div class="block">
                <p>
                    The Screen Wake Lock API enables web apps to prevent devices from dimming or locking the screen when the app needs to keep running.
                </p>
                <button
                    class="button button-fill"
                    data-action="click->pwa--wake-lock#lock"
                >
                    <twig:ux:icon name="material-symbols:lock-outline" class="icon size-20 margin-right-half" fill="currentColor" />
                    Lock
                </button>
                <button
                    class="button button-fill color-red"
                    data-action="click->pwa--wake-lock#release"
                >
                    <twig:ux:icon name="material-symbols:lock-open-outline" class="icon size-20 margin-right-half" fill="currentColor" />
                    Release
                </button>
            </div>

        </div>

    </div>
</template>

<script>
    export default function(props, {$, $el, $f7, $f7route, $f7router, $h, $on, $store, $theme, $update}) {
        return $render;
    }
</script>
