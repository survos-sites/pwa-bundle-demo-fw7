<template>
    <div class="page">

        <div class="navbar">
            <div class="navbar-bg"></div>
            <div class="navbar-inner">
                <div class="left">
                    <a href="#" class="link back">
                        <i class="icon icon-back"></i>
                        <span class="ios-only">Back</span>
                    </a>
                </div>
                <div class="title">Speech synthesis</div>
            </div>
        </div>

        <div class="page-content">

            <div class="block-title block-title-large">Speech synthesis</div>
            <div class="block">
                <p>
                    Click on the button below to hear a synthesized voice.
                </p>
                <p>
                    TO BE COMPLETED
                </p>
                <input
                    type="text"
                    id="text"
                    name="text"
                    placeholder="Enter text to speak"
                />
                <select
                    id="voice"
                    name="voice"
                >
                    <option value="Google français">Google français</option>
                </select>
                <select
                    id="locale"
                    name="locale"
                >
                </select>
                <input
                    type="number"
                    id="rate"
                    name="rate"
                    placeholder="Rate"
                />
                <input
                    type="number"
                    id="pitch"
                    name="pitch"
                    placeholder="Pitch"
                />

                <button
                    class="button button-fill"
                    data-controller="speech"
                    {{ stimulus_action('speech', 'speak', 'click', {
                        text: 'Hi developers! Welcome to the PWA Bundle. This is a demo of the Speech Synthesis API.',
                        voice: 'Google English',
                        locale: 'en-US',
                        rate: 1,
                        pitch: 0.7
                    }) }}
                >Listen
                </button>
            </div>

        </div>

    </div>
</template>

<script>
    export default function(props, {$, $el, $f7, $f7route, $f7router, $h, $on, $store, $theme, $update}) {
        return $render;
    }
</script>
